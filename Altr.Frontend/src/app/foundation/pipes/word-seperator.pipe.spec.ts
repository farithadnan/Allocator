import { WordSeperatorPipe } from './word-seperator.pipe';

describe('WordSeperatorPipe', () => {
  it('create an instance', () => {
    const pipe = new WordSeperatorPipe();
    expect(pipe).toBeTruthy();
  });
});
